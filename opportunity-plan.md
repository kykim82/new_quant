# 거래대금 우선 기회 탐색 v5 계획.

기존 D:\kywork\New_quant와 사용자 Cloudflare Worker를 유지한다. 넓은 손절폭을 자동 탈락시키지 않고, 위험과 목표 여력을 사용자가 함께 판단하게 한다.

1. 전체 시세 → 거래대금 선별 → 1시간·4시간 구조 → 15분 진입의 단계별 수집으로 변경한다.
   - 검증. 낮은 거래대금 종목은 상세 호출을 사용하지 않으며, 저장 계획은 계속 추적한다. 호출 예산과 오래 기다린 대상의 순서를 테스트한다.
2. 10억 공통 기준과 3일 연속 증가/1시간 급증 예외, 완성 봉·일봉 배경 및 전환/눌림 신호를 구현한다.
   - 검증. 하락 중 반등 차단, 초기 전환 허용, 손절 2%/6% 초과 허용, 고정 가격 보존 회귀 테스트.
3. 상세 분석 대상만 관찰에 표시하고, 위험·기존 매수가와 현재가에서의 잔여 목표 여력을 구분한다.
   - 검증. 관찰 제외/지연 집계/API 호환성, 타입 검사와 빌드.
4. 검증된 소스를 의미 단위 커밋하고 기존 GitHub→Cloudflare 경로로 반영한다.
   - 검증. 원격 API 버전과 응답, 소스 ZIP 파일명·경로 안내. 계정이 다른 로컬 wrangler로 배포하지 않는다.
