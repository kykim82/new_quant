# 작업 컨텍스트 기록

## 2026-09-05 요구사항 결정

- 대상은 업비트 원화마켓이다.
- 결과는 15분봉 단타와 1시간~4시간봉 스윙으로 분리한다.
- 각 후보에 매수가, 손절가, 1차·2차·3차 매도가를 표시한다.
- 1차 범위는 추천 전용이며 자동주문은 포함하지 않는다.
- 모바일과 PC에서 보는 반응형 웹앱·PWA로 만든다.
- Cloudflare에 비공개 배포한다.
- 로컬 최종 프로젝트 위치는 `D:\kywork\New_quant`이다.

## 2026-09-05 전략 결정

- 공통 하드 필터 뒤에 전략별 점수를 적용한다.
- 단타는 1시간 추세 확인과 15분 거래대금 동반 20봉 고점 돌파를 기본 패턴으로 한다.
- 스윙은 4시간 추세와 1시간 눌림·돌파 진입을 결합한다.
- 핵심 지표는 EMA, RSI, ATR, ADX/DMI, PPO, RVOL, CMF, Donchian이다.
- 목표가는 위험 단위 R과 실제 저항을 함께 사용한다.
- 완성 봉만 사용하고 비용과 슬리피지를 반영한다.

## 2026-09-05 배포 결정

- 브라우저가 업비트 API를 직접 호출하지 않는다.
- Cloudflare Worker가 서버 측에서 수집·분석하고 화면은 저장된 결과를 읽는다.
- D1에는 최신 분석 결과와 지난 신호를 저장한다.
- 예약 실행은 캔들 마감 뒤에 수행하며, 실패하면 마지막 정상 결과를 유지한다.
- 공개 API만 사용하므로 업비트 API 키는 필요하지 않다.

## 2026-09-05 구현 및 검증 기록

- 원화마켓 전체 티커를 선별한 뒤 Cloudflare 외부 요청 한도 안에서 거래대금 상위 9개를 심층 분석한다.
- 기본 스캔은 외부 요청 약 40회이며 요청 단위 재시도는 하지 않는다. 실패 시 저장된 마지막 정상 결과를 유지한다.
- 캔들 요청 일부 실패는 종목 단위로 격리하되, BTC 기준봉이 없거나 전체의 70% 미만만 완료되면 새 결과를 저장하지 않는다.
- 업비트가 무거래 봉을 생략하는 특성을 반영해 내부 및 마지막 완성 시각까지 결측 봉을 채우고 최근 결측이 있으면 추천에서 제외한다.
- 현재가가 계산된 진입 구간에서 ATR 허용 폭 이상 벗어나면 추격 또는 실패 신호로 보고 추천하지 않는다.
- 2025년 7월 31일 이후 원화마켓 가격 구간별 호가 단위를 적용하고 호가 정책 API의 문자열 응답을 숫자로 정규화한다.
- 화면은 1분 주기와 가시성 복귀 시 최신 저장 결과를 확인하며, 네트워크 오류가 나면 만료 후보를 제거하고 지연 상태를 표시한다.
- 실제 업비트 연동에서 원화마켓 287개, 유동성 기준 통과 29개, 심층 분석 9개, 완료 9개를 확인했다.
- 로컬 D1에서 실시간 결과 저장 후 동일 생성 시각의 캐시 조회를 확인했다.
- 타입 검사와 린트가 통과했고 단위 테스트 19개가 통과했다.
