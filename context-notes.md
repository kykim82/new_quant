# 작업 컨텍스트 기록

## 2026-09-06 거래대금·전환 기회 v5.

- 배포 검증. 사용자 Worker에서 02:48:08 KST의 schemaVersion 5, stale=false, error=null을 확인했다. 상세 81개 중 최신 47개/첫 분석 대기 34개/분석 지연 0개였다. 초기 순회 중이며 수익성 우위나 항상 추천이 발생함을 보장하지 않는다. 코드 커밋 43af290, 소스 ZIP release/upbit_recommender.zip, 기존 ZIP 백업 release/upbit_recommender-pre-opportunity-v5.zip.

- 사용자 승인으로 단타 2%·스윙 6% 및 ATR 배수의 손절폭 탈락 제한을 제거했다. 가격 순서/유효성, 미완성·누락·지연 데이터, 주의·경고 시장과 체결 데이터 검사는 유지한다.
- 24h 10억을 단타·스윙 공통 상세 대상 기준으로 적용한다. 최근 비중복 24h 3구간 연속 증가 및 첫 구간 대비 1.5배, 또는 최근 완성 1h가 이전 20h 평균의 2배이고 1천만원 이상이면 예외 편입한다. 수익 성과로 최적화된 수치가 아닌 초기 정책이다.
- 기본 감시 대상은 매분 최대 12개에 대해 1h 캔들만 확인한다. 전체 24h 시세는 매 실행 확인한다. 순간 체결 스트림이 아니므로 1h 급증은 봉 마감 및 순회 후 확정된다. 고거래대금/기존 계획과 최근 증가 예외는 상세 큐로 갱신한다.
- 전체 800봉 백필을 운영 경로에서 제거했다. 최신 페이지 1회/시간대와 기존 캐시를 합치며 오래된 캐시는 최대 800봉 보존한다. TT 이력이 부족하면 준비 중으로 표시하고 구조 목표를 사용한다. TT 알고리즘은 수정하지 않는다.
- 1h/4h 상승 구조와 초기 저점 상승·확정 고점 회복을 확인하며, 통과하지 못하면 15m 신규 진입 분석을 건너뛴다. 기존 활성 가격 계획의 15m 손절/목표 관측은 계속한다. 일봉은 완성 UTC 4h 6봉으로 합성한 배경이며 하드 게이트가 아니다.
- 86개 테스트 통과. 41개 상세 대상+20개 저거래대금의 모의 환경에서 5회 이내 상세 범위를 순회하고 매 실행 외부 호출 45회 이하, 저거래대금에는 1h만 호출함을 검증했다. 실서비스 갱신시간 보장은 아니다.
- 로컬 실제 API에서 v5, 전체 287개/상세 81개/기본 감시 182개/첫 실행 신규 분석 38개 및 지연 0을 확인했다. 그 시점 진입 후보 0개이며 수익성 개선을 입증한 결과는 아니다.
- 기존 D1 JSON 필드 확장으로 적용하여 별도 원격 스키마 변경이나 데이터 삭제가 없다. 새 모의 기록은 opportunity-v5로 분리한다.
- 세부 작업 이력은 opportunity-context-notes.md 및 opportunity-checklist.md에 기록한다.

## 고정 계획 요청 결정.

- 운영 검증. 코드93b1a86을 기존 GitHub main에 반영했다. 운영 화면 자산 dashboard-Ape6Op9x.js와 API schemaVersion4/generatedAt1788626528000/stale=false/error=null을 확인했다. 당시 저장 계획0개이며 실제 계획 생명주기는 SQLite 모의 통합 및 단위 검사로 검증했다. 기존 ZIP은 release/upbit_recommender-pre-fixed-plan.zip으로 보존하고 upbit_recommender.zip을 갱신했다.
- 검증. 73개 테스트와 typecheck/lint/build를 통과했다. SQLite 통합 검사에서 탈락·만료·재시작·캐시 지연 이후에도 최초 ID와 목표가를 보존했다. 로컬 API schemaVersion4, stale=false, error=null을 확인했다.
- UI는 기존 구성을 유지하면서 저장 가격 계획과 신규 진입 대기 사유를 추가했다. 순회 차례 밖 종목도 관측된 현재가 손절·목표 도달을 저장한다. 완료된15분봉 저가도 검사하되 손절과 같은 가격은 하회가 아니다. 초단위 체결 전수 관측은 아니므로 누락 없는 실시간 손절 감시를 보장하지 않는다.
- 신규 진입 검증 유효시간은 저장 가격의 기존 만료와 별도다. 모의 기록은 fixed-plan-v4로 분리했고 실제 계좌 보유를 자동 판단하지 않는다. 기존 모의 보유기한·청산 규칙은 변경하지 않았다.
- 사용자가 가격 계획 고정과 신규 진입 판단 분리를 승인했다. 후보 탈락이나 시간 만료가 저장 계획 삭제로 이어지지 않게 한다.
- 매도 목표 생성 수식은 변경하지 않는다. 현재 엔진은 구조 고점·피벗·ABC·TT 가격을 가까운 구간으로 병합해 순서대로 최대3개 선택한다. 5% 최소 목표는 없다.
- 종목·전략당 마지막 계획을 저장한다. 손절 하회가 관측된 계획은 종료하고, 이후 새 완성봉 신호만 새 계획으로 교체한다. 목표 선도달은 신규 진입 대기 사유로 보존하며 되돌림에도 자동 해제하지 않는다.
- 추세·진입·호가 조건이 부족해도 계획은 보존한다. 최근 확인을 놓친 공백은 임의로 정상 취급하지 않는다. 실제 계좌 보유 여부는 알 수 없으므로 보유 포지션으로 표시하지 않는다.

## 스프레드 주의 표시 전환 결정

## 2026-09-06 구조 기반 엔진 구현 결정

- 배포 검증. 코드9e7f758을 사용자 GitHub main에 푸시했다. 운영 dashboard-Bt_ZKiRL.js에서 새 가격 근거·산정 대기 문구를 확인했고 API generatedAt1788624968000/schemaVersion3/stale=false/error=null을 확인했다. 최초 새 이력 완료4/263종목, 나머지는 순차 재분석 중이다. 업로드 ZIP은 release/upbit_recommender.zip이며 이전 파일은 upbit_recommender-pre-v3.zip으로 보존했다.
- 구현 검증. 62개 테스트, typecheck, lint, build 통과. 로컬 빌드 Worker에서 페이지·출처 문서 HTTP200, 업비트 실제 API schemaVersion3/stale=false/error=null, 263개 대상 중 최초3개 이력 완료를 확인했다.
- 최소399개 실제 봉과 TT 준비 상태를 모두 확인한다. 첫 목표가 비용 또는 호가 반올림 후 진입 구간에 붙으면 더 먼 목표로 건너뛰지 않는다. ABC 기준 파동은 최소2ATR, 전체 군집 폭은 max(2틱,0.15ATR) 이내다.
- 테스트의 단타 정상 사례가 전일R1과 진입 상단이 같은 호가가 되어 NO_RESISTANCE_ROOM으로 거절됐다. 이는 의도한 보호 동작이므로 해당 정상 fixture의 전일 변동폭을 수정했고, 근접 저항 거절은 별도 테스트로 유지한다.
- 증분 계산과 일괄 계산의 동일성은 합성 입력으로 검증했다. 사용자 TradingView 실제 설정·상장 이후 전체 이력과의 수치 대조나 수익성 개선은 아직 검증하지 않았다. ST는 상위봉 방향 필터이며 모의 청산의 동적 추적 손절은 이번 구현에 추가하지 않았다.
- 사용자가 검토안 구현을 승인했다. 개인용 전제와 원본 저작자·라이선스 표시를 유지한다. 이번 작업은 기존 사용자 GitHub/Cloudflare만 갱신한다.
- TT 기본 TrendLength10/SetTargets0,ST ATR10×3 Wilder를 사용한다. 초기800봉 후 증분 상태를 D1 result_json에 보존한다. 이력 증가는 최초 전체 준비 시간을 늘리므로 UI에 표시한다.
- 전일·전주 OHLC는 완전한 UTC4시간봉6개/42개로 집계한다. 결측 기간은 피벗을 생성하지 않는다. 구조 고점·ABC는 오른쪽2봉 확인 이후에만 쓴다.
- 목표는 최대3개 숫자 배열로 바꾸고 부족한 항목은 산정 대기다. 5% 최소 수익 강제·AVWAP·Volume Profile은 이번 필수 구현 범위에 추가하지 않는다. 미래 매도 비용은 가정으로 표시한다.
- 기존 전략은 가격 계획 비교용 보존본으로 남긴다. 현행 후보에서 손익비 하한·손익비 점수 기여를 제거한다. 위험 한도·데이터·경고종목·체결 물량 보호는 유지한다.

- 코드 커밋72740f9를 기존 GitHub main에 반영했다. 운영 dashboard-CeLfcBon.js의 '호가 스프레드 · 참고용'과 API generatedAt=1788619988000의 새 notice,stale=false,error=null을 확인했다. 로컬 CLI의 기본 계정은 사용자 Worker 계정이 아니므로 CLI 직접 배포는 하지 않았다. 기존 GitHub 연동 배포를 사용했다.
- 배포 확인 이전 API는23:31경 저장된 지연 결과였으나 이후 새 정책 결과로 갱신됐다. 원격 상태를 수정하거나 잠금을 강제 삭제하지 않았다.

- 구현 후 42개 테스트, 타입 검사, 린트, 프로덕션 빌드를 통과했다. 단타·스윙의 2% 스프레드 입력에서도 합격·점수·가격 동일, 경고 갱신·해제, 물량·슬리피지 보호를 검증했다.
- 첨부 main.py 1491~1546행. TT 상단=최근10봉 고가 평균+0.8ATR, 하단=최근10봉 저가 평균-0.8ATR. 하락→상승 전환 봉 종가를 entry로 저장하고 전환 봉 하단을 stop으로 보존한다. ATR 기간은15분/1시간21,4시간/일봉14이다. 이후 entry/stop은 유지하지만 최신ATR로 목표를 재계산한다. 목표=entry+4/8/12ATR.
- main.py 1966~1999행의 주 추천표·성과 기록은 15분 level_price_columns를 실제 사용한다. 1627~1662행의 process_target_data는 20봉 저점 매수,50봉 고점·1.618확장·일봉R2의 최댓값 추세목표를 별도로 표시한다. 주 추천표의1~3차 목표와 혼동하지 않는다.
- TT 값이 유효하지 않으면 level_price_columns는 현재가와 SuperTrend/2ATR/6% 손절,1.2/2/3R 목표로 보정한다. 따라서 원본은 완전히 R을 배제한 설계가 아니다. TP3 기준 최소RR1.35,최소목표0.8%,최대손절12%도 검사한다.
- 이식 의견. 변동성 기반 목표는 손절과 독립이라는 장점이 있지만4/8/12ATR 배수 자체는 저항 도달의 근거가 아니다. 오래된 진입가·움직이는 목표를 그대로 쓰지 않고 현재 유효 진입, 신호 시점 고정 목표, 실제 상위봉 저항과 비용 후 여력을 검증해야 한다. 이번에는 이식을 하지 않는다.
- 배포 직후 저장된 구버전 종목 결과는 다음 순회 분석 때 새 점수·탈락 기준으로 교체된다. 현재 후보의 스프레드 경고는 매 집계의 새 호가로 즉시 갱신한다.

- 사용자는 스프레드만 주의로 표시하도록 요청했다. 개선 전략의 스프레드 탈락과 점수 기여를 없애고 잔여 점수를 100점으로 환산한다. 기준 비교용 legacy-strategy는 원형 보존한다.
- 0.2% 단타·0.35% 스윙은 제외 기준이 아닌 주의 문구 표시 기준으로만 사용한다. 최신 호가에 따라 경고를 교체하며 다른 경고는 보존한다.
- 물량 부족·슬리피지·손익비와 목표가 계산은 이번 범위에서 변경하지 않는다. 사용자가 원하지 않는 위험 배수 목표가는 별도 설계 변경 과제로 남긴다.
- 첨부 ZIP은 읽기 전용 검토한다. 주 추천표는 level_price_columns(15m)의 TT 가격을 사용하며 process_target_data의 추세 목표가와 구분해야 한다.

## 2026-09-05 요구사항 결정

- 대상은 업비트 원화마켓이다.
- 결과는 15분봉 단타와 1시간~4시간봉 스윙으로 분리한다.
- 각 후보에 매수가, 손절가, 1차·2차·3차 매도가를 표시한다.
- 1차 범위는 추천 전용이며 자동주문은 포함하지 않는다.
- 모바일과 PC에서 보는 반응형 웹앱·PWA로 만든다.
- Cloudflare에 비공개 배포한다.
- 로컬 최종 프로젝트 위치는 `D:\kywork\New_quant`이다.

## 2026-09-05 전략 결정

- 공통 하드 필터 뒤에 전략별 점수를 적용한다.
- 단타는 1시간 추세 확인과 15분 거래대금 동반 20봉 고점 돌파를 기본 패턴으로 한다.
- 스윙은 4시간 추세와 1시간 눌림·돌파 진입을 결합한다.
- 핵심 지표는 EMA, RSI, ATR, ADX/DMI, PPO, RVOL, CMF, Donchian이다.
- 목표가는 위험 단위 R과 실제 저항을 함께 사용한다.
- 완성 봉만 사용하고 비용과 슬리피지를 반영한다.

## 2026-09-05 배포 결정

- 브라우저가 업비트 API를 직접 호출하지 않는다.
- Cloudflare Worker가 서버 측에서 수집·분석하고 화면은 저장된 결과를 읽는다.
- D1에는 최신 분석 결과와 지난 신호를 저장한다.
- 예약 실행은 캔들 마감 뒤에 수행하며, 실패하면 마지막 정상 결과를 유지한다.
- 공개 API만 사용하므로 업비트 API 키는 필요하지 않다.

## 2026-09-05 구현 및 검증 기록

- 원화마켓 전체 티커를 선별한 뒤 Cloudflare 외부 요청 한도 안에서 거래대금 상위 9개를 심층 분석한다.
- 기본 스캔은 외부 요청 약 40회이며 요청 단위 재시도는 하지 않는다. 실패 시 저장된 마지막 정상 결과를 유지한다.
- 캔들 요청 일부 실패는 종목 단위로 격리하되, BTC 기준봉이 없거나 전체의 70% 미만만 완료되면 새 결과를 저장하지 않는다.
- 업비트가 무거래 봉을 생략하는 특성을 반영해 내부 및 마지막 완성 시각까지 결측 봉을 채우고 최근 결측이 있으면 추천에서 제외한다.
- 현재가가 계산된 진입 구간에서 ATR 허용 폭 이상 벗어나면 추격 또는 실패 신호로 보고 추천하지 않는다.
- 2025년 7월 31일 이후 원화마켓 가격 구간별 호가 단위를 적용하고 호가 정책 API의 문자열 응답을 숫자로 정규화한다.
- 화면은 1분 주기와 가시성 복귀 시 최신 저장 결과를 확인하며, 네트워크 오류가 나면 만료 후보를 제거하고 지연 상태를 표시한다.
- 실제 업비트 연동에서 원화마켓 287개, 유동성 기준 통과 29개, 심층 분석 9개, 완료 9개를 확인했다.
- 로컬 D1에서 실시간 결과 저장 후 동일 생성 시각의 캐시 조회를 확인했다.
- 타입 검사와 린트가 통과했고 단위 테스트 19개가 통과했다.

## 2026-09-05 최종 배포 기록

- 프로덕션 의존성뿐 아니라 개발 의존성까지 npm 보안 취약점 0건을 확인했다.
- 커밋 `326814b8dd228073678450b6386ca77255436b37`에서 Sites 버전 1을 만들었다.
- 소유자 1명만 허용되고 외부 방문자와 그룹이 없는 비공개 정책으로 배포했다.
- 배포 주소는 `https://krw-quant-radar.kykim82.chatgpt.site`이다.
- 프로덕션 홈과 추천 API가 HTTP 200을 반환하고, 실시간 결과가 D1에 저장된 뒤 캐시로 다시 조회되는 것을 확인했다.
- Worker 로그에서 실제 시장 스캔과 화면 요청이 모두 성공한 것을 확인했다.
# Cloudflare 사용자 계정 D1 연결

## 2026-09-05 개선 결정

- 배포 커밋 a221334가 사용자 GitHub main과 new-quant.kykim82.workers.dev에 반영됐다. 운영 API schemaVersion=2, 오류 없음과 매분 자동 진행10→30→50종목을 확인했다. generatedAt은1788617108000→1788617228000, 분석 수30→50으로 사용자 POST 없이 진행했다. 초기 전체 수집은 계속 진행 중이다.
- 최종36개 테스트와 타입·정적 검사·빌드 통과. 소스 ZIP은 release/upbit_recommender.zip이며 이전 ZIP은 release/upbit_recommender-pre-v2.zip으로 보관했다.
- 최초 푸시 자동 승인 검토가 목적지 증거 부족으로 거절했으나, 사용자가 기존에 만든 github 원격과 인증된 원격main=9455e97을 확인해 재검토 후 정상 푸시했다.

- SQLite 통합 검사에서 저거래대금 21개 전체를 3회 실행으로 처리하고 외부 요청 최대45회를 확인했다. 실행 간 잠금·경고 제외·현시세 재검사·모의 체결 순서 검증을 추가했다.
- 실제 로컬 Worker에서 10→20종목으로 이어지는 D1 순회와 관찰37건, 스윙3건을 확인했다. 기존 기준은 같은 데이터에서1건이었으나 성과 우위를 의미하지 않는다.
- 기존 전략은 legacy-strategy.ts로 보존했다. 동일 전체시장 대상으로 병행 비교하므로 기존9종목 제한을 포함한 원본 운용 성과 재현이 아니다.
- 분할 분석 초기 준비는 여러 실행에 걸치고 이후 캐시 갱신 속도도 시장·호출 실패에 좌우된다. 진행률과20분 이상 지연 종목,3분 이상 지연된 대시보드의 매수 후보 차단으로 상태를 드러낸다.

- 사용자가 첨부 quant-terminal 비교 개선안의 구현을 승인했다. 원본 첨부 프로그램은 변경하지 않는다.
- 공식 D1 ID와 사용자 GitHub/Workers 배포는 유지한다. 원격 테이블은 앱이 CREATE TABLE IF NOT EXISTS로 준비하여 수동 SQL 단계 없이 전환한다.
- 무료 Worker 단일 실행 외부 요청 50회 제약 안에서 캔들 호출을 예산화한다. 전체 종목 수집은 순회로 진행하며 첫 준비 시간과 지연을 숨기지 않는다.
- 거래대금은 분석 대상 자체를 배제하지 않고 전략별 진입 허용에만 사용한다. 하한 단타 10억·스윙 5억은 검증 전 초기값이다.
- 검토와 편집은 C 드라이브 임시 작업본에서 하고 검증 대상 소스만 사용자 지정 D:\kywork\New_quant에 복사한다.

- 2026-09-05. 사용자 Cloudflare 계정에 `new-quant` D1 데이터베이스를 Asia Pacific 위치로 생성했다.
- D1 바인딩 `DB`는 사용자 데이터베이스 ID `e980ee48-f1d7-4a21-9255-31c7a47bb4de`를 사용한다.
