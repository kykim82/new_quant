# 업비트 원화마켓 추천 대시보드 개발 계획

## 2026-09-06 구조 기반 가격 엔진 구현

1. 원본 기본값의 증분 Supertrend·Target Trend와 800봉 초기 이력을 구현한다. 검증은 분할 재시작과 일괄 계산 동일성·고정 목표·완성 봉이다.
2. 확정 고점·전일/전주 피벗·ABC 확장·TT 후보를 병합하고 독립 손절과 순서 있는 최대3개 목표를 산정한다. 가격 계획을 버전·ID로 고정하고 미래 데이터 불변을 검사한다.
3. 현재 전략의 가격 산정·손익비 중심 필터를 교체하고 스프레드 주의는 유지한다. 새 스키마로 구 후보를 격리하고 모의 기록은 confluence-v3로 분리한다.
4. 카드·차트·상세에 목표 근거·수익 여력·산정 대기·지표 상태를 연결한다. 테스트·타입·빌드·로컬 Worker 확인 후 기존 GitHub/Cloudflare와 ZIP을 갱신한다.

## 스프레드 주의 표시 전환과 첨부 가격 산정 검토

1. 스프레드를 개선 전략의 탈락·감점에서 제거하고 기존 점수 범위를 100점으로 환산한다. 검증은 단타·스윙 동일 입력의 스프레드 변화가 합격과 점수에 영향이 없는지 확인한다.
2. 최신 호가로 주의 문구를 갱신하고 카드·상세에 표시한다. 물량 부족·슬리피지·데이터 누락 보호는 유지하고 저장 후보 재검사를 검증한다.
3. 첨부 quant-terminal-main.zip의 실제 호출 경로와 매수·목표가 공식을 확인한다. 이번에는 목표가 로직을 변경하지 않는다.
4. 전체 테스트·타입·빌드 통과 후 기존 사용자 GitHub/Cloudflare로 반영하고 ZIP 위치를 안내한다.

## 2026-09-05 전체시장 분석 개선

- 목적. 9종목 고정 제한을 없애고 주의·경고 제외 전체 종목을 영속 커서로 순회한다.
- 설계. 1분 예약 실행, 실행별 외부 호출 예산, 시간대별 캔들 캐시, D1 작업 잠금으로 중복 수집을 방지한다. 첫 전체 수집은 여러 실행에 걸치며 실제 진행률과 오래된 분석을 표시한다.
- 전략. 단타 10억·스윙 5억 원 하한, 단타 눌림 회복 추가, RSI·ADX 일부 점수화. 호가·손익비·위험회피는 필수 조건으로 유지한다.
- 표시. 진입 후보와 관찰 후보 분리, 종목별 미충족 사유, 데이터 시각과 범위 표시.
- 검증. 스케줄 순환·호출 예산·만료·주의종목 재검사·전략 회귀 테스트, 빌드, D1 로컬 통합 검증 후 GitHub 자동 배포를 확인한다.
- 성과. 기존 엄격 전략과 개선 전략을 동일 데이터로 비교하고, 모의 진입 체결 이후 목표/손절 평가를 별도 기록한다. 동일 봉 동시 도달은 불명확으로 분류한다.

## 목표

업비트 공개 시세를 이용해 원화마켓의 15분 단타와 1~4시간 스윙 후보를 분리 분석하고, 모바일과 PC에서 확인할 수 있는 비공개 Cloudflare 웹 대시보드를 만든다.

## 전제

- 1차 버전은 추천·분석 전용이며 실제 주문은 실행하지 않는다.
- 계산에는 완전히 종료된 캔들만 사용한다.
- 기준 미달이면 후보를 억지로 채우지 않고 추천 없음으로 표시한다.
- 업비트 API 키를 브라우저나 저장소에 넣지 않는다.
- 배포본은 기본적으로 소유자만 접근 가능한 비공개 상태로 둔다.

## 구현 단계와 검증

1. Sites 프로젝트와 Cloudflare 호환 실행 환경을 구성한다.
   - 검증: 개발 서버가 정상 응답하고 기본 빌드가 성공한다.
2. 업비트 공개 API 수집기와 호출 제한·결측·캐시 처리를 구현한다.
   - 검증: 고정 샘플과 실제 공개 API에서 페어·티커·캔들·호가를 정상화한다.
3. 기술지표와 단타·스윙 추천 규칙을 순수 함수로 구현한다.
   - 검증: EMA, RSI, ATR, ADX, PPO, RVOL, CMF, Donchian과 가격 산정 단위 테스트가 통과한다.
4. D1에 최신 분석 결과와 지난 신호를 저장하는 서버 API를 구현한다.
   - 검증: 빈 데이터, 최신 데이터, 지연 데이터 응답을 각각 확인한다.
5. 모바일 우선 반응형 PWA 대시보드를 구현한다.
   - 검증: 단타·스윙 탭, 후보 카드, 상세 차트, 로딩·빈 상태·오류 상태가 동작한다.
6. 예약 분석과 수동 새로고침 경로를 연결한다.
   - 검증: 동일 캔들 중복 분석 방지와 실패 후 마지막 정상 결과 유지가 동작한다.
7. 전체 테스트와 프로덕션 빌드를 통과시킨다.
   - 검증: 테스트, 타입 검사, 빌드가 모두 성공한다.
8. 비공개 Cloudflare 사이트로 배포하고 결과물을 압축한다.
   - 검증: 배포 상태 성공과 배포 URL 응답을 확인한다.

## 산출물

- 프로젝트 위치: `D:\kywork\New_quant`
- 업로드 파일: `D:\kywork\New_quant\release\upbit_recommender.zip`
- 배포 결과: 비공개 Cloudflare URL
